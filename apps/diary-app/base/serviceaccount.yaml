apiVersion: v1
kind: ServiceAccount
metadata:
  name: diary-app-sa
  namespace: diary-app
  annotations:
    # Terraform으로 생성한 IAM Role ARN으로 교체 필요
    # eks.amazonaws.com/role-arn: arn:aws:iam::<AWS_ACCOUNT_ID>:role/team4-diary-app-irsa
